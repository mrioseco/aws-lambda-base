test('plantilla inicial index', async () => {

    console.log(`Ya que los scripts hacen test antes de deployar o pushear, es necesario tener al menos 1 test para que proceso no falle.`);
        
    expect('ok').toContain('ok');     
});